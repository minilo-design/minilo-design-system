import React from 'react';
import DocumentSelection from './DocumentSelection';

export default {
  title: 'Components/Inputs/Document Selection',
  component: DocumentSelection,
};

export const Default = () => <DocumentSelection />;
